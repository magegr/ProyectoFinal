{# templates/panel.html.twig - plantilla completa, sense extends ni blocks #}
<!DOCTYPE html>
<html lang="ca">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Panell | Òptica Adam</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        :root{
            --navy:#0D2B45;
            --bg:#dfeaf6;
            --border: rgba(13,43,69,.12);
            --text: rgba(13,43,69,.78);
        }

        body{
            background-color: var(--bg);
            color: var(--text);
        }

        header .brand-title{
            color: var(--navy);
            letter-spacing: .3px;
            font-weight: 800;
        }

        header .brand-subtitle{
            color: rgba(13,43,69,.75);
            margin: 0;
        }

        .panel-card{
            background: #fff;
            border: 1px solid var(--border);
            border-radius: 22px;
            box-shadow: 0 1rem 2.5rem rgba(13,43,69,.10);
        }

        .panel-btn{
            width: 100%;
            text-align: left;
            background: #fff;
            border: 1px solid rgba(13,43,69,.15);
            border-radius: 16px;
            padding: 18px 18px;
            transition: transform .15s ease, box-shadow .15s ease, border-color .15s ease;
        }

        .panel-btn:hover{
            transform: translateY(-2px);
            box-shadow: 0 .8rem 2rem rgba(13,43,69,.12);
            border-color: var(--bg);
            background-color: #dfeaf6;
        }

        .panel-btn strong{
            display:block;
            color: var(--navy);
            font-size: 1.05rem;
        }

        .panel-btn span{
            display:block;
            color: rgba(13,43,69,.75);
            margin-top: 4px;
            font-size: .95rem;
        }

        .pill{
            display:inline-block;
            margin-top: 10px;
            background: var(--bg);
            color: var(--navy);
            border: 1px solid rgba(13,43,69,.18);
            border-radius: 999px;
            padding: 6px 12px;
            font-weight: 700;
            font-size: .85rem;
        }

        footer{
            color: rgba(13,43,69,.65);
            font-size: .9rem;
        }
    </style>
</head>

<body>
<main class="mt-5 pb-5">
    <div class="container">
        <section class="panel-card p-4 p-md-5" aria-label="Navegació del panell">
            <div class="row g-3">
                <div class="col-12">
                    <a class="btn panel-btn" href="">
                        <strong>Usuaris</strong>
                        <span>Gestiona usuaris</span>
                        <span class="pill">Anar a usuaris</span>
                    </a>
                </div>

                <div class="col-12">
                    <a class="btn panel-btn" href="">
                        <strong>Productes</strong>
                        <span>Catàleg, estoc, preus i imatges.</span>
                        <span class="pill">Anar a productes</span>
                    </a>
                </div>

                <div class="col-12">
                    <a class="btn panel-btn" href="">
                        <strong>Cites</strong>
                        <span>Agenda i reserves</span>
                        <span class="pill">Anar a cites</span>
                    </a>
                </div>
            </div>
        </section>
    </div>
</main>

<footer class="py-4">
    <div class="container text-center">
        <small>© Òptica Adam</small>
    </div>
</footer>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
